using System.Collections;
using System.Collections.Generic;
using JetBrains.Annotations;
using UnityEngine;

public class Pattern : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;
        // Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern; Pattern isPattern; Pattern isNotPattern;
        // if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern; if (this is Pattern) Pattern pattern = this is Pattern ? isPattern : isNotPattern;

    }


}
